syntax = "proto3";

package onight.zjfae.afront.gens;

//Generated By BrewRobot:Thu Apr 21 20:22:32 CST 2016

message PBIFE_messagemanage_listMessage{
	///ife/messagemanage/listMessage.html
	message MessageListList{
		string id = 1;	 //,编号,String,不校验,是,
		string sender = 2;	 //,发送者id,String,不校验,是,
		string recivier = 3;	 //,接收者id,String,不校验,是,
		string msgType = 4;	 //,消息类型,String,不校验,是,0-系统消息；1-私信
		string msgStatus = 5;	 //,消息状态,String,不校验,是,0-正常；1-删除；2-过期
		string readStatus = 6;	 //,阅读状态,String,不校验,是,0-未读；1-已读
		string title = 7;	 //,标题,String,不校验,是,
		string content = 8;	 //,内容,String,不校验,是,
		string createTime = 9;	 //,创建时间,String,不校验,是,
		string updateTime = 10;	 //,更新时间,String,不校验,是,
		string expTime = 11;	 //,失效时间,String,不校验,是,
		string contentId = 12;	 //,额外消息号,String,不校验,是,若消息内容不够，需要额外表消息保存，关联此字段；默认为0不关联
		string createTimeStart = 13;	 //,查询起始日,String,不校验,是,
		string createTimeEnd = 14;	 //,查询终止日,String,不校验,是,
	}

	message PageInfo{
		string pageSize = 1;	 //,页大小,String,不校验,是,
		string pageIndex = 2;	 //,当前页,String,不校验,是,
		string pageCount = 3;	 //,总页数,String,不校验,是,
		string totalCount = 4;	 //,总条数,String,不校验,是,
	}


	PageInfo pageInfo = 1; //,消息列表,List,不校验,是,

	repeated MessageListList messageListList = 2;


}

message Ret_PBIFE_messagemanage_listMessage{
	///ife/messagemanage/listMessage.html

	string returnCode=1;	//报文头

	string returnMsg=2;	//报文头

	repeated PBIFE_messagemanage_listMessage data=3;

	repeated REQ_PBIFE_messagemanage_listMessage data1=4;


}

message REQ_PBIFE_messagemanage_listMessage{
	///ife/messagemanage/listMessage.html

	string startDate = 1; //,开始日期,String,不校验,否,

	string endDate = 2; //,结束日期,String,不校验,否,

	string content = 3; //,消息内容,String,不校验,否,

	string pageIndex = 4; //,当前页,String,不校验,否,

	string pageSize = 5; //,页大小,String,不校验,否,


}